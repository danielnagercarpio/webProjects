<?php
/**
 * operators
 */

 $x = 10;
 $y = 2;

 // Elevate
 var_dump($x ** $y);
 // Aritmethic operators (+ - / * ** %)
// It converts it in negative number
 var_dump(-$x);
 var_dump($x / 0); // It throws and Uncaught DivisionByZeroError
 // fdiv(x, y) -> returns an infinity instead of zero division error
$a += 3; // Undefined variable warning


// String operators (.  .=)

